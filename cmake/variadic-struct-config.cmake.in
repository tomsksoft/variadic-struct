# The following import targets are created in Tomsksoft:: namespace:
#
#  varstruct
#
# This module also sets the following variables in your project:
#
#   variadic-struct_FOUND
#   variadic-struct_INCLUDE_DIR

@PACKAGE_INIT@

if (NOT TARGET varstruct)
	include("${CMAKE_CURRENT_LIST_DIR}/@package_config_prefix@-targets.cmake")

	if (TARGET Tomsksoft::varstruct)
		get_target_property(@PROJECT_NAME@_INCLUDE_DIR Tomsksoft::varstruct INTERFACE_INCLUDE_DIRECTORIES)
	endif()
endif()
